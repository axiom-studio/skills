# Document Renderer Skill

Installs the `openseal.document` action. It renders a reviewed, cited report
into transient PDF bytes; the authorized runtime persists those bytes as a
durable artifact and exposes only the artifact reference to the Agent.
