# Governed Email Delivery Skill

Installs the `openseal.delivery` action. Provider configuration is supplied as
an opaque credential at execution time, attachment bytes are verified against
durable artifact metadata, and receipts contain no credential material.
