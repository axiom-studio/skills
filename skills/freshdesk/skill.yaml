apiVersion: openseal.dev/v1alpha1
definition:
    actions:
        freshdesk-agent-list:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Agent List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    domain:
                        description: Freshdesk domain
                        type: string
                required:
                    - domain
                type: object
            name: freshdesk-agent-list
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        freshdesk-article-list:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Article List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    categoryId:
                        description: Filter by category ID
                        type: integer
                    domain:
                        description: Freshdesk domain
                        type: string
                required:
                    - domain
                type: object
            name: freshdesk-article-list
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        freshdesk-contact-list:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Contact List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    domain:
                        description: Freshdesk domain
                        type: string
                required:
                    - domain
                type: object
            name: freshdesk-contact-list
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        freshdesk-solution-create:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Solution Create
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    categoryId:
                        description: Category ID
                        type: integer
                    description:
                        description: Article content
                        type: string
                    domain:
                        description: Freshdesk domain
                        type: string
                    folderId:
                        description: Folder ID
                        type: integer
                    status:
                        default: 1
                        description: Article status
                        type: integer
                    title:
                        description: Article title
                        type: string
                required:
                    - categoryId
                    - description
                    - domain
                    - folderId
                    - title
                type: object
            name: freshdesk-solution-create
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        freshdesk-ticket-create:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Ticket Create
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    description:
                        description: Ticket description
                        type: string
                    domain:
                        description: Freshdesk domain
                        type: string
                    email:
                        description: Requester email
                        type: string
                    priority:
                        default: 1
                        description: Priority (1-4)
                        type: integer
                    status:
                        default: 2
                        description: Status (2=Open, 3=Pending, 4=Resolved)
                        type: integer
                    subject:
                        description: Ticket subject
                        type: string
                required:
                    - description
                    - domain
                    - email
                    - subject
                type: object
            name: freshdesk-ticket-create
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        freshdesk-ticket-list:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Ticket List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    domain:
                        description: Freshdesk domain
                        type: string
                    priority:
                        description: Filter by priority
                        type: integer
                    status:
                        description: Filter by status
                        type: string
                    userId:
                        description: Filter by requester ID
                        type: string
                required:
                    - domain
                type: object
            name: freshdesk-ticket-list
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        freshdesk-ticket-reply:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Ticket Reply
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    body:
                        description: Reply body
                        type: string
                    domain:
                        description: Freshdesk domain
                        type: string
                    ticketId:
                        description: Ticket ID
                        type: integer
                required:
                    - body
                    - domain
                    - ticketId
                type: object
            name: freshdesk-ticket-reply
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        freshdesk-ticket-update:
            credentials:
                - kind: freshdesk-credential
                  name: freshdesk-credential
            description: Freshdesk Ticket Update
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    description:
                        description: New description
                        type: string
                    domain:
                        description: Freshdesk domain
                        type: string
                    priority:
                        description: New priority
                        type: integer
                    status:
                        description: New status
                        type: integer
                    subject:
                        description: New subject
                        type: string
                    ticketId:
                        description: Ticket ID
                        type: integer
                required:
                    - domain
                    - ticketId
                type: object
            name: freshdesk-ticket-update
            permissions:
                - freshdesk:read
                - freshdesk:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
    category: communication
    description: Freshdesk helpdesk operations for Atlas agents
    icon: life-buoy
    id: skill-freshdesk
    installers:
        - id: oci
          kind: oci
          label: Freshdesk Skill service image
          package: axiomstudio/skill-freshdesk:1.0.0
    name: Freshdesk Skill
    source:
        format: axiom.skill/v1
        homepage: ""
        license: MIT
        publisher: Axiom Studio
        reference: skill-freshdesk
        resolvedVersion: 1.0.0
    tags:
        - freshdesk
        - helpdesk
        - support
        - tickets
    transport:
        endpoint: skill-freshdesk
        kind: tool
    version: 1.0.0
diagnostics:
    - code: forward_ported
      message: Forward-ported into the canonical OpenSeal Skill contract with conservative action policy.
      severity: info
kind: SkillDefinition
