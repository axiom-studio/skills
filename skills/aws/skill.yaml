apiVersion: openseal.dev/v1alpha1
definition:
    actions:
        aws-ec2-list-instances:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Ec2 List Instances
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                type: object
            name: aws-ec2-list-instances
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        aws-ec2-reboot-instance:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Ec2 Reboot Instance
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    instanceId:
                        description: EC2 instance ID
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - instanceId
                type: object
            name: aws-ec2-reboot-instance
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: production
            sideEffect: external
        aws-ec2-start-instance:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Ec2 Start Instance
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    instanceId:
                        description: EC2 instance ID
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - instanceId
                type: object
            name: aws-ec2-start-instance
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: production
            sideEffect: external
        aws-ec2-stop-instance:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Ec2 Stop Instance
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    instanceId:
                        description: EC2 instance ID
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - instanceId
                type: object
            name: aws-ec2-stop-instance
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: production
            sideEffect: external
        aws-lambda-get-function:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Lambda Get Function
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    functionName:
                        description: Lambda function name
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - functionName
                type: object
            name: aws-lambda-get-function
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        aws-lambda-invoke:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Lambda Invoke
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    functionName:
                        description: Lambda function name
                        type: string
                    payload:
                        description: Lambda invocation payload
                        type: object
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - functionName
                type: object
            name: aws-lambda-invoke
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: production
            sideEffect: external
        aws-lambda-list-functions:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws Lambda List Functions
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                type: object
            name: aws-lambda-list-functions
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        aws-s3-delete-object:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws S3 Delete Object
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    bucket:
                        description: S3 bucket name
                        type: string
                    key:
                        description: S3 object key
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - bucket
                    - key
                type: object
            name: aws-s3-delete-object
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: destructive
            sideEffect: destructive
        aws-s3-get-object:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws S3 Get Object
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    bucket:
                        description: S3 bucket name
                        type: string
                    key:
                        description: S3 object key
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - bucket
                    - key
                type: object
            name: aws-s3-get-object
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        aws-s3-list-buckets:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws S3 List Buckets
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                type: object
            name: aws-s3-list-buckets
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        aws-s3-list-objects:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws S3 List Objects
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    bucket:
                        description: S3 bucket name
                        type: string
                    prefix:
                        description: Key prefix filter
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - bucket
                type: object
            name: aws-s3-list-objects
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        aws-s3-put-object:
            credentials:
                - kind: aws_credentials
                  name: aws_credentials
            description: Aws S3 Put Object
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    bucket:
                        description: S3 bucket name
                        type: string
                    content:
                        description: Object content
                        type: string
                    contentType:
                        default: application/octet-stream
                        description: Content type
                        type: string
                    key:
                        description: S3 object key
                        type: string
                    region:
                        default: us-east-1
                        description: AWS region
                        type: string
                required:
                    - bucket
                    - content
                    - key
                type: object
            name: aws-s3-put-object
            permissions:
                - aws:read
                - aws:write
            retry:
                maxAttempts: 1
            risk: production
            sideEffect: external
    category: cloud
    description: AWS cloud operations (S3, EC2, Lambda) for Atlas agents
    icon: cloud
    id: skill-aws
    installers:
        - id: oci
          kind: oci
          label: AWS Skill service image
          package: axiomstudio/skill-aws:1.0.0
    name: AWS Skill
    source:
        format: axiom.skill/v1
        homepage: ""
        license: MIT
        publisher: Axiom Studio
        reference: skill-aws
        resolvedVersion: 1.0.0
    tags:
        - aws
        - cloud
        - ec2
        - lambda
        - s3
    transport:
        endpoint: skill-aws
        kind: tool
    version: 1.0.0
diagnostics:
    - code: forward_ported
      message: Forward-ported into the canonical OpenSeal Skill contract with conservative action policy.
      severity: info
kind: SkillDefinition
