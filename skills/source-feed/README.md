# Source Feed Observer Skill

Installs the `openseal.source` action for recurring RSS and Atom observation.
The runtime must inject an exact, scope-bound source-policy decision; the Skill
rejects arbitrary URLs, private network targets, credential-bearing URLs, and
policy drift.
