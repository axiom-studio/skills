apiVersion: openseal.dev/v1alpha1
definition:
    actions:
        sonar-branches-list:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Branches List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    projectKey:
                        description: Project key
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - projectKey
                    - url
                type: object
            name: sonar-branches-list
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        sonar-issue-assign:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Issue Assign
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    assignee:
                        description: Assignee user
                        type: string
                    issueKey:
                        description: Issue key
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - assignee
                    - issueKey
                    - url
                type: object
            name: sonar-issue-assign
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        sonar-issues-list:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Issues List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    projectKey:
                        description: Project key
                        type: string
                    severities:
                        description: Filter by severities
                        type: string
                    statuses:
                        description: Filter by statuses
                        type: string
                    types:
                        description: Filter by types
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - projectKey
                    - url
                type: object
            name: sonar-issues-list
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        sonar-measures:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Measures
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    metrics:
                        description: Metrics to retrieve
                        type: string
                    projectKey:
                        description: Project key
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - projectKey
                    - url
                type: object
            name: sonar-measures
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        sonar-project-list:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Project List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    organization:
                        description: Organization key
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - url
                type: object
            name: sonar-project-list
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        sonar-quality-gate:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Quality Gate
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    projectKey:
                        description: Project key
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - projectKey
                    - url
                type: object
            name: sonar-quality-gate
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        sonar-rules-list:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Rules List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    repository:
                        description: Rules repository
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - url
                type: object
            name: sonar-rules-list
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        sonar-scan:
            credentials:
                - kind: sonarqube-credential
                  name: sonarqube-credential
            description: Sonar Scan
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    branch:
                        description: Branch name
                        type: string
                    projectKey:
                        description: Project key
                        type: string
                    url:
                        description: SonarQube URL
                        type: string
                required:
                    - projectKey
                    - url
                type: object
            name: sonar-scan
            permissions:
                - sonarqube:read
                - sonarqube:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
    category: security
    description: SonarQube code quality analysis for Atlas agents
    icon: search
    id: skill-sonarqube
    installers:
        - id: oci
          kind: oci
          label: SonarQube Skill service image
          package: axiomstudio/skill-sonarqube:1.0.0
    name: SonarQube Skill
    source:
        format: axiom.skill/v1
        homepage: ""
        license: MIT
        publisher: Axiom Studio
        reference: skill-sonarqube
        resolvedVersion: 1.0.0
    tags:
        - analysis
        - code-quality
        - security
        - sonarqube
    transport:
        endpoint: skill-sonarqube
        kind: tool
    version: 1.0.0
diagnostics:
    - code: forward_ported
      message: Forward-ported into the canonical OpenSeal Skill contract with conservative action policy.
      severity: info
kind: SkillDefinition
