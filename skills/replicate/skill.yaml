apiVersion: openseal.dev/v1alpha1
definition:
    actions:
        replicate-model-get:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Model Get
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    model:
                        description: Model name (owner/name)
                        type: string
                required:
                    - model
                type: object
            name: replicate-model-get
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        replicate-model-list:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Model List
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties: {}
                type: object
            name: replicate-model-list
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        replicate-prediction-cancel:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Prediction Cancel
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    predictionId:
                        description: Prediction ID
                        type: string
                required:
                    - predictionId
                type: object
            name: replicate-prediction-cancel
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        replicate-prediction-status:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Prediction Status
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    predictionId:
                        description: Prediction ID
                        type: string
                required:
                    - predictionId
                type: object
            name: replicate-prediction-status
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
        replicate-run:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Run
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    input:
                        description: Model input parameters
                        type: object
                    model:
                        description: Model name (owner/name)
                        type: string
                    version:
                        description: Model version
                        type: string
                    wait:
                        default: false
                        description: Wait for completion
                        type: boolean
                required:
                    - input
                    - model
                type: object
            name: replicate-run
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        replicate-training-create:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Training Create
            idempotency: required
            inputSchema:
                additionalProperties: false
                properties:
                    destination:
                        description: Destination model
                        type: string
                    input:
                        description: Training input parameters
                        type: object
                    model:
                        description: Model name (owner/name)
                        type: string
                required:
                    - destination
                    - input
                    - model
                type: object
            name: replicate-training-create
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: external
            sideEffect: external
        replicate-training-status:
            credentials:
                - kind: replicate-credential
                  name: replicate-credential
            description: Replicate Training Status
            idempotency: supported
            inputSchema:
                additionalProperties: false
                properties:
                    trainingId:
                        description: Training ID
                        type: string
                required:
                    - trainingId
                type: object
            name: replicate-training-status
            permissions:
                - replicate:read
                - replicate:write
            retry:
                maxAttempts: 1
            risk: read
            sideEffect: read
    category: ai
    description: Replicate ML model hosting operations for Atlas agents
    icon: cpu
    id: skill-replicate
    installers:
        - id: oci
          kind: oci
          label: Replicate Skill service image
          package: axiomstudio/skill-replicate:1.0.0
    name: Replicate Skill
    source:
        format: axiom.skill/v1
        homepage: ""
        license: MIT
        publisher: Axiom Studio
        reference: skill-replicate
        resolvedVersion: 1.0.0
    tags:
        - inference
        - ml
        - models
        - replicate
    transport:
        endpoint: skill-replicate
        kind: tool
    version: 1.0.0
diagnostics:
    - code: forward_ported
      message: Forward-ported into the canonical OpenSeal Skill contract with conservative action policy.
      severity: info
kind: SkillDefinition
