[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "axiomstudio-skill-browser"
version = "2.0.32"
description = "Governed Camoufox stealth browser automation skill"
requires-python = ">=3.11"
