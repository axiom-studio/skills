[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "axiomstudio-skill-camoufox"
version = "1.0.10"
description = "Governed Camoufox stealth browser automation skill"
requires-python = ">=3.11"
