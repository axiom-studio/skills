apiVersion: openseal.dev/v1alpha1
definition:
    actions:
        camoufox-health:
            name: camoufox-health
            description: Report governed Camoufox automation readiness without exposing target, profile, or proxy configuration.
            inputSchema: {type: object, additionalProperties: false}
            permissions: [browser:session]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
        camoufox-start:
            name: camoufox-start
            description: Start an isolated persistent Camoufox session against one configured target using opaque profile and egress identities.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    targetId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    path: {type: string, pattern: '^/', maxLength: 2048, default: /}
                    profileId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    proxyPoolId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                required: [sessionId, targetId, profileId, proxyPoolId]
            permissions: [browser:session, network:http]
            idempotency: none
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
            timeout: 45000000000
        camoufox-navigate:
            name: camoufox-navigate
            description: Navigate an active session to an HTTP(S) URL while preserving its browser context and cookies; optionally opt into a configured target/path guard.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    url: {type: string, pattern: '^https?://', maxLength: 2048}
                    targetId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    path: {type: string, pattern: '^/', maxLength: 2048, default: /}
                required: [sessionId]
                oneOf:
                    - required: [url]
                    - required: [targetId]
            permissions: [browser:session, network:http]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
            timeout: 45000000000
        camoufox-snapshot:
            name: camoufox-snapshot
            description: Capture a compact viewport-focused semantic snapshot and typed access challenges from the current page. Optionally attach a bounded screenshot for a vision-capable model.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    includeScreenshot: {type: boolean, default: false}
                required: [sessionId]
            permissions: [browser:observe]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
        camoufox-follow-link:
            name: camoufox-follow-link
            description: Follow one exact anchor from the current snapshot within the active authorized target without activating a form or mutation control.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    target: {type: string, pattern: '^s[1-9][0-9]*:e[1-9][0-9]*$'}
                    intent: {type: string, minLength: 3, maxLength: 500}
                required: [sessionId, target, intent]
            permissions: [browser:session, browser:observe, network:http]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
            timeout: 45000000000
        camoufox-click:
            name: camoufox-click
            description: Click one exact current snapshot reference without committing a stable external business operation.
            inputSchema: &interaction_input
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    target: {type: string, pattern: '^s[1-9][0-9]*:e[1-9][0-9]*$'}
                    generation: {type: integer, minimum: 1}
                    x: {type: number, minimum: 0, maximum: 10000}
                    'y': {type: number, minimum: 0, maximum: 10000}
                    intent: {type: string, minLength: 3, maxLength: 500}
                    writeAuthorized: {type: boolean}
                    idempotencyKey: {type: string, minLength: 8, maxLength: 128}
                required: [sessionId, intent, writeAuthorized, idempotencyKey]
                oneOf:
                    - required: [target]
                    - required: [generation, x, 'y']
            permissions: [browser:interact, network:http]
            externalOperationPolicy: forbidden
            idempotency: required
            retry: {maxAttempts: 1}
            risk: external
            sideEffect: external
        camoufox-commit:
            name: camoufox-commit
            description: Click the final control that commits one approved, stable, externally observable operation.
            inputSchema: *interaction_input
            permissions: [browser:interact, network:http]
            externalOperationPolicy: required
            idempotency: required
            retry: {maxAttempts: 1}
            risk: external
            sideEffect: external
        camoufox-fill:
            name: camoufox-fill
            description: Click and type into one exact current non-secret control with human cadence.
            inputSchema: &fill_input
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    target: {type: string, pattern: '^s[1-9][0-9]*:e[1-9][0-9]*$'}
                    value: {type: string, maxLength: 20000}
                    intent: {type: string, minLength: 3, maxLength: 500}
                    writeAuthorized: {type: boolean}
                    idempotencyKey: {type: string, minLength: 8, maxLength: 128}
                required: [sessionId, target, value, intent, writeAuthorized, idempotencyKey]
            permissions: [browser:interact]
            externalOperationPolicy: forbidden
            idempotency: required
            retry: {maxAttempts: 1}
            risk: external
            sideEffect: external
            semanticArguments: {body: value, target: target}
        camoufox-fill-secret:
            name: camoufox-fill-secret
            description: Fill one exact login control from an authorized credential binding without exposing the value.
            credentials:
                - {kind: http_basic_auth, name: username}
                - {kind: http_basic_auth, name: password}
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    target: {type: string, pattern: '^s[1-9][0-9]*:e[1-9][0-9]*$'}
                    credentialField: {type: string, enum: [username, password]}
                    intent: {type: string, minLength: 3, maxLength: 500}
                    writeAuthorized: {type: boolean}
                    idempotencyKey: {type: string, minLength: 8, maxLength: 128}
                required: [sessionId, target, credentialField, intent, writeAuthorized, idempotencyKey]
            permissions: [browser:interact, credential:use]
            externalOperationPolicy: forbidden
            idempotency: required
            retry: {maxAttempts: 1}
            risk: external
            sideEffect: external
            semanticArguments: {target: target}
        camoufox-select:
            name: camoufox-select
            description: Set one exact native select control to a value or visible label.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    target: {type: string, pattern: '^s[1-9][0-9]*:e[1-9][0-9]*$'}
                    value: {type: string, maxLength: 1000}
                    intent: {type: string, minLength: 3, maxLength: 500}
                    writeAuthorized: {type: boolean}
                    idempotencyKey: {type: string, minLength: 8, maxLength: 128}
                required: [sessionId, target, value, intent, writeAuthorized, idempotencyKey]
            permissions: [browser:interact]
            externalOperationPolicy: forbidden
            idempotency: required
            retry: {maxAttempts: 1}
            risk: external
            sideEffect: external
            semanticArguments: {target: target}
        camoufox-scroll:
            name: camoufox-scroll
            description: Scroll the current viewport by a bounded pixel delta to reveal lazy content without risking an unbounded pointer operation.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    dx: {type: number, default: 0}
                    dy: {type: number, default: 0}
                required: [sessionId]
            permissions: [browser:observe]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
        camoufox-screenshot:
            name: camoufox-screenshot
            description: Capture a bounded PNG screenshot of the current viewport or full page as evidence.
            inputSchema:
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                    fullPage: {type: boolean, default: false}
                required: [sessionId]
            permissions: [browser:observe]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
        camoufox-report:
            name: camoufox-report
            description: Return accepted, challenged, or blocked outcome evidence without revealing profile, proxy, cookie, or credential values.
            inputSchema: &session_input
                type: object
                additionalProperties: false
                properties:
                    sessionId: {type: string, pattern: '^[A-Za-z0-9][A-Za-z0-9_-]{0,63}$'}
                required: [sessionId]
            permissions: [browser:observe, security:report]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: read
        camoufox-close:
            name: camoufox-close
            description: Release an isolated local automation session while preserving its configured browser profile; this does not change the remote site.
            inputSchema: *session_input
            permissions: [browser:session]
            idempotency: supported
            retry: {maxAttempts: 1}
            risk: read
            sideEffect: none
    category: security
    description: Governed Camoufox stealth browser automation using opaque authorized targets, OS-level fingerprint profiles, and rotating egress identities.
    icon: shield
    id: skill-browser
    installers:
        - id: oci
          kind: oci
          label: Camoufox Skill service image with pinned Camoufox and Firefox
          package: axiomstudio/skill-browser:2.0.3
    name: Browser Skill
    prompt:
        allowedTools:
            - camoufox-health
            - camoufox-start
            - camoufox-navigate
            - camoufox-snapshot
            - camoufox-follow-link
            - camoufox-click
            - camoufox-commit
            - camoufox-fill
            - camoufox-fill-secret
            - camoufox-select
            - camoufox-scroll
            - camoufox-screenshot
            - camoufox-report
            - camoufox-close
        instructions: Use configured target, profile, and proxy-pool identifiers to start sessions. Follow ordinary anchors from the current snapshot with camoufox-follow-link; it remains inside the active authorized target and does not require write authority. Use camoufox-navigate with an HTTP(S) URL only when no current link exists, or provide targetId when strict configured origin/path scope is desired. Take a new snapshot after every interaction or navigation. Use camoufox-click for mutation-capable controls and camoufox-commit only for the final approved external operation. A third-party access challenge requires human completion; pause automation when one is reported.
        userInvocable: true
    requirements:
        compute:
            limits: {cpu: 2000m, memory: 2Gi}
            requests: {cpu: 250m, memory: 512Mi}
        operatingSystems: [linux]
        storage:
            - name: camoufox-profile
              durability: persistent
              minimumCapacity: 2Gi
              mountPath: /var/lib/openseal-camoufox
              retention: retain
              writableGroup: 1000
    source:
        format: axiom.skill/v1
        homepage: https://github.com/daijro/camoufox
        license: MIT
        publisher: Axiom Studio
        reference: skill-browser
        resolvedVersion: 2.0.3
    tags: [anti-detect, browser, camoufox, firefox, stealth]
    transport:
        endpoint: skill-browser
        kind: tool
    version: 2.0.3
kind: SkillDefinition
